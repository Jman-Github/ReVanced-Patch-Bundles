{
  "created_at": "2026-05-15T01:58:10",
  "description": "## BannerHub v6 1.2.0-604\n\nA patched build of XiaoJi GameHub 6.0.4 (`com.xiaoji.egggame`, versionCode 114) that removes the login requirement so games can be imported and used without an account, redirects the catalog API to the BannerHub Cloudflare Worker, and ships **9 variants that install side-by-side on the same device**.\n\n### \u2728 What's new in 1.2.0-604\n\nA full **privacy hardening stack** plus a small **UX fix** for the Explorer-view More Menu. Drop-in update on top of `v1.1.0-604` \u2014 cert unchanged, installs in place.\n\n#### \ud83d\udd10 Privacy hardening \u2014 7 functional patches + a public doc\n\nEvery telemetry channel listed below is now **silent on the device**. The complete data-flow write-up \u2014 including what we deliberately did NOT touch and why \u2014 lives in [`PRIVACY.md`](https://github.com/The412Banner/bannerhub-revanced/blob/gamehub-604-build/PRIVACY.md) at the repo root.\n\n| Telemetry channel | What it leaked | Mechanism |\n| --- | --- | --- |\n| **Firebase Analytics** | screen views, session starts, in-app purchases, custom events \u2192 `app-measurement.com` | Manifest `<meta-data>` kill switch (`firebase_analytics_collection_deactivated=true` + AD-ID/SSAID disables); the SDK never initializes |\n| **Mob Push SDK** | device identifiers, push tokens, lifecycle events \u2192 Mob | Bytecode removes 3 SDK init call sites + manifest disables every `com.mob.*` / `cn.fly.*` component (ContentProvider auto-init can't fire either) |\n| **AD-ID / ADSERVICES permissions** | advertising-ID exposure to any caller that queries Play Services | 3 `<uses-permission>` declarations stripped |\n| **XiaoJi OTA update phone-home** | firmware-update check \u2192 `xiaoji.com/firmware/update/x1` | URL register rewritten to `127.0.0.1`; per-arch OTA native libs stripped |\n| **Heartbeat / playtime tracker** | per-game playtime sessions \u2192 XiaoJi via `heartbeat/game/{start,update,end}` | Suspend lambda bodies replaced with `return Unit.INSTANCE`; `getUserPlayTimeList` returns an empty wrapped list |\n| **GMS Measurement** | persistent `app_instance_id`, active `session_id`, lifecycle pauses \u2192 Google | Three GMS manifest components (`AppMeasurementReceiver` / `AppMeasurementService` / `AppMeasurementJobService`) flipped to `android:enabled=\"false\"` |\n| **`statistic-gamehub-api.vgabc.com/events`** + **`/events/device-performance-config`** | general analytics events + device-perf telemetry \u2192 XiaoJi | Pure client-side stub: `Lcx5;->a` and `Loh4;->b` early-return fake success instances before any URL string is allocated |\n\nEmpirically verified on a real device during dev: a full 6.5-minute session (install \u2192 open \u2192 game launch \u2192 in-game session \u2192 quit) produced **zero DNS queries** for `statistic-gamehub-api.vgabc.com` and **zero advancement** of GMS Measurement's `last_pause_time` field.\n\n> \u26a0 **We're not pretending nothing leaves the device.** [`PRIVACY.md`](https://github.com/The412Banner/bannerhub-revanced/blob/gamehub-604-build/PRIVACY.md) honestly enumerates what's still visible: image CDNs (`bigeyes.com`, `shared.akamai.steamstatic.com`), Firebase's vestigial settings-config GET, GOG Galaxy telemetry, and the BannerHub Cloudflare Worker that serves the catalog API. Read it.\n\n#### \ud83c\udf9b PC Game Settings \u2014 now always visible in Explorer view\n\nVanilla GameHub hides the \"PC Game Settings\" row in the game-detail More Menu for Steam-linked games (Steam manages its own settings; the raw Wine/DXVK/Box64/VKD3D dialog wouldn't apply). This release **forces that row to always appear** regardless of game type or launch method.\n\nSingle-instruction bytecode patch: removes the `if-eqz` gate immediately before the row's construction in `Lx57;->a()`. Other More Menu rows (PC Uninstall, Online Update, Instant Settings, Version Switch, etc.) keep their native gating \u2014 only PC Game Settings is forced visible.\n\nCaveat: tapping PC Game Settings for a Steam-launched game opens the Wine settings dialog, but Steam wraps the executable so the settings won't reach the actual Steam process. Useful for direct-launch PC games regardless of backing source.\n\n#### Carryover from `v1.1.0-604`\n\nStable keystore, PC-accurate XInput rumble (`MODE_CONTROLLER` 100% by default), PC Vibration Settings menu row in both per-game popups, BannerHub v6 visual rebrand, and the in-place-update behaviour all carry forward unchanged.\n\n### \ud83d\udd10 Signing \u2014 in-place updates from `v1.1.0-604` onward\n\nThis release is signed with the **same stable test keystore** ([`keystore/bannerhub.keystore`](https://github.com/The412Banner/bannerhub-revanced/blob/gamehub-604-build/keystore/README.md)) introduced in `v1.1.0-604` \u2014 cert SHA-256 `10:89:5A:31:1F:E0:4F:95:F8:2E:4D:A5:C9:A6:C0:41:BA:92:82:BF:21:1F:1B:57:8F:E1:CB:EB:89:4C:E0:BA`. Cross-check against `apksigner verify --print-certs` output in any of this release's CI logs.\n\n- **Upgrading from `v1.1.0-604`**: install the matching variant on top \u2014 Android accepts the update.\n- **Upgrading from `v1.0.0-604` or older**: those used per-run ephemeral keys, so the cert differs. **Uninstall once**, then install the new variant. From there on out updates flow normally.\n\n### \u2705 Steam game launches work end-to-end\n\nStandard Steam client and Lightweight Steam client both launch games successfully \u2014 the BannerHub Cloudflare Worker serves a Steam-launch-compatible payload for both. Pick whichever Steam client suits your title.\n\n> \u26a0 **If your first Steam launch errors with `xtask install components failed`:** open that game's **PC game settings** (now always available \u2014 see the new section above), set the components manually (Wine prefix, DXVK, VKD3D, container, etc.) and make sure the **correct Steam client** is selected, then try to launch again. One-time setup; subsequent launches succeed.\n\n### Source\n\n- **Base APK**: `GameHub_6.0.4.apk` \u2014 the official 6.0.4 global build (versionCode 114), attached unmodified to [`base-apk-604`](https://github.com/The412Banner/bannerhub-revanced/releases/tag/base-apk-604).\n- **Patcher**: ReVanced CLI 6.0.0 + this repo's patch bundle, branch `gamehub-604-build`.\n- **Catalog backend**: [`The412Banner/bannerhub-api`](https://github.com/The412Banner/bannerhub-api) Cloudflare Worker at `bannerhub-api.the412banner.workers.dev`.\n\n### Variants\n\nNine APKs are produced from the same patch bundle, each with a different package name + launcher label so they install side-by-side. The `Original` variant **replaces** an installed GameHub; the rest install **alongside** the original.\n\n| Variant | APK | Package | Launcher label |\n| --- | --- | --- | --- |\n| Normal | `BannerHub-V6-1.2.0-604-Patched-Normal.apk` | `banner.hub` | BannerHub v6 |\n| Normal-GHL | `BannerHub-V6-1.2.0-604-Patched-Normal-GHL.apk` | `gamehub.lite` | BannerHub v6 |\n| PuBG | `BannerHub-V6-1.2.0-604-Patched-PuBG.apk` | `com.tencent.ig` | BannerHub v6 PuBG |\n| AnTuTu | `BannerHub-V6-1.2.0-604-Patched-AnTuTu.apk` | `com.antutu.ABenchMark` | BannerHub v6 AnTuTu |\n| alt-AnTuTu | `BannerHub-V6-1.2.0-604-Patched-alt-AnTuTu.apk` | `com.antutu.benchmark.full` | BannerHub v6 AnTuTu |\n| PuBG-CrossFire | `BannerHub-V6-1.2.0-604-Patched-PuBG-CrossFire.apk` | `com.tencent.tmgp.cf` | BannerHub v6 PuBG CrossFire |\n| Ludashi | `BannerHub-V6-1.2.0-604-Patched-Ludashi.apk` | `com.ludashi.aibench` | BannerHub v6 Ludashi |\n| Genshin | `BannerHub-V6-1.2.0-604-Patched-Genshin.apk` | `com.miHoYo.GenshinImpact` | BannerHub v6 Genshin |\n| Original | `BannerHub-V6-1.2.0-604-Patched-Original.apk` | `com.xiaoji.egggame` | BannerHub v6 |\n\n<details>\n<summary><strong>\ud83d\udce6 Patches applied (click to expand)</strong></summary>\n\n| Patch | What it does |\n| --- | --- |\n| **Bypass login** | Lets you launch straight into the home screen and import games without an account. |\n| **Disable Firebase Crashlytics** | Removes the Crashlytics init block so the patched build doesn't crash on launch with `VerifyError`. |\n| **Debug logging** | Marks the APK debuggable and adds logcat markers along the import code path for triage. |\n| **File manager access** | Exposes a content provider so external file managers (MT Manager etc.) can browse GameHub's data dir without root. |\n| **Rewrite custom permissions per variant** | Per-variant `com.xiaoji.egggame.permission.*` rewrite so two installed variants don't collide on Android 7+. |\n| **Mute UI sounds** | Replaces the bundled menu/button click sounds with silent PCM. Game audio is untouched. |\n| **Redirect catalog API** | Points the catalog API at the BannerHub Cloudflare Worker (`bannerhub-api.the412banner.workers.dev`) for curated component delivery. Side benefit: per-game PC settings now render in both landscape and portrait. |\n| **Prefix API path with /v6** | Prepends `v6/` to every relative API call so the Worker can serve 6.0-only response variants without affecting 5.x clients. |\n| **Offline component cache fallback** \u26a0 *currently broken on 6.0.4* | *Intended to let per-game pickers show your cached components when the device is offline.* Online behavior is unchanged, so this caveat only matters when you're offline. Investigation pending; safe to ignore unless you specifically rely on offline picker contents. |\n| **PC-accurate vibration** | 4 bytecode hooks routing XInput rumble into Android's VibratorManager with dual-motor independent dispatch on multi-motor pads. SDL2's 1 s auto-stop defeated by a guest-side `libevshim.so` shim that re-issues `SDL_JoystickRumble` every 500 ms. Defaults to `MODE_CONTROLLER` at 100%. |\n| **Vibration native shim** | Ships an arm64-v8a `libevshim.so` (~41 KB) into the APK; loaded into Wine via LD_PRELOAD. Patches `winebus.so`'s `pSDL_JoystickRumble` / `pSDL_JoystickClose` pointers at runtime. |\n| **Vibration settings activity** | Registers `com.xj.winemu.vibration.BhVibrationSettingsActivity` (internal, exported=false). Per-game dialog: mode (off / device / controller / both) + intensity (0-100%). |\n| **PC Vibration Settings menu row** | Injects a 5th menu row labelled **PC Vibration Settings** into both per-game popups. Launches the vibration settings dialog scoped to the active game. |\n| **PC Vibration Settings label resource** | Appends a `bh_pc_vibration_label` entry to `features.home`'s Compose Multiplatform `.cvr` bundle (with a bytecode-patched runtime resolver short-circuit). |\n| **Change app icon** | Replaces 5 in-APK drawables with BannerHub v6 branding (adaptive-icon foreground, `wine_logo`, auth/splash banners). |\n| **Disable Firebase Analytics** \u2b50 *new in 1.2.0-604* | Manifest kill switch: `firebase_analytics_collection_deactivated=true` + AD-ID/SSAID disables. Firebase Analytics SDK never initializes; no events sent to `app-measurement.com`. |\n| **Disable Mob Push tracking** \u2b50 *new in 1.2.0-604* | Removes 3 Mob SDK init invocations in XiaoJi bootstrap (`BaseAndroidApp` + helper) plus manifest `android:enabled=\"false\"` on every `com.mob.*` / `cn.fly.*` `<provider>`/`<service>`/`<receiver>`/`<activity>`. ContentProvider auto-init can't fire either. |\n| **Strip Ad-ID permissions** \u2b50 *new in 1.2.0-604* | Removes `AD_ID`, `ACCESS_ADSERVICES_ATTRIBUTION`, `ACCESS_ADSERVICES_AD_ID` from `<uses-permission>` declarations. Strengthens the Firebase Analytics disable above. |\n| **Disable OTA updates** \u2b50 *new in 1.2.0-604* | Overwrites the OTA URL register with `http://127.0.0.1` after the const-string load in `ki4.smali`, so `xiaoji.com/firmware/update/x1` is unreachable. Per-arch `libJieLiUsbOta.so` + `libjl_ota_auth.so` natives are stripped from the APK. |\n| **Disable heartbeat** \u2b50 *new in 1.2.0-604* | Disables XiaoJi's server-side playtime tracker (`heartbeat/game/{start,update,end}` + `getUserPlayTimeList`). All four methods return early so no network call fires and no per-tick work runs. Trade-off: in-app playtime UI will be empty. |\n| **Disable GMS Measurement** \u2b50 *new in 1.2.0-604* | Flips `android:enabled` to `false` on the three `com.google.android.gms.measurement.*` manifest components. Complementary to Disable Firebase Analytics \u2014 GMS Measurement runs as a separate Play Services component and would otherwise keep writing `app_instance_id` + `session_id` to shared_prefs. |\n| **Stub analytics events** \u2b50 *new in 1.2.0-604* | Pure-stub neutralization of XiaoJi's two analytics-event reporters (`Lcx5;->a` general `/events` POST batches + `Loh4;->b` `/events/device-performance-config`). Both early-return fake success instances before any URL is built. Zero outbound traffic to `statistic-gamehub-api.vgabc.com`. |\n| **Show PC Game Settings row** \u2b50 *new in 1.2.0-604* | Removes the `if-eqz` gate in front of the PC Game Settings row in the Explorer-view More Menu so the option appears for every game type, including Steam-linked games. |\n| **Change package name** *(per variant)* | Renames the APK package per variant so all 9 variants install side-by-side. The `Original` variant keeps `com.xiaoji.egggame`. |\n| **Change app name** *(per variant)* | Renames the launcher label per variant. |\n\n*Per-patch mechanics, smali edits, structural anchors, and version-by-version letter maps live in the [README's Patches applied section](https://github.com/The412Banner/bannerhub-revanced/blob/gamehub-604-build/README.md#patches-applied) and the patch source comments under [`patches/src/main/kotlin/app/revanced/patches/gamehub/`](https://github.com/The412Banner/bannerhub-revanced/tree/gamehub-604-build/patches/src/main/kotlin/app/revanced/patches/gamehub).*\n\n</details>\n\n### Notes\n\n- The base APK is unmodified XiaoJi GameHub 6.0.4; only the bytecode/resources required by the patches above are touched.\n- **Signing**: signed with the stable BannerHub test keystore (cert SHA-256 `10895a311fe04f95f82e4da5c9a6c041ba9282bf211f1b578fe1cbeb894ce0ba`). One-time uninstall required if upgrading from `v1.0.0-604` or older (different cert). From this release onward, all future BannerHub v6 stables install in place. Cert fingerprints: see [`keystore/README.md`](https://github.com/The412Banner/bannerhub-revanced/blob/gamehub-604-build/keystore/README.md).\n- The `.rvp` patch bundle and `.rve` extension files are also attached for use with `revanced-cli` directly.\n",
  "download_url": "https://github.com/The412Banner/bannerhub-revanced/releases/download/v1.2.0-604/patches.rvp",
  "signature_download_url": "N/A",
  "version": "v1.2.0-604"
}