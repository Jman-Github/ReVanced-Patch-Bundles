name: Patch bundle updater

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'

jobs:
  update_bundle:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
          persist-credentials: false
          fetch-depth: 0
    
    - name: Set up generate_bundles.py
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
      
    - name: Run generate_bundles.py
      run: |
        pip install httpx
        python generate_bundles.py
      
    - name: Check for changes
      id: check_changes
      run: |
        git diff --quiet || echo "Changes found"
      continue-on-error: true
      
    - name: Finalize and commit changes
      if: steps.check_changes.outputs.stdout != ''
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add *.json
        git commit -a -m "updated patch-bundle.json to latest"
        git pull --rebase
        
    - name: Push changes
      if: steps.check_changes.outputs.stdout != ''
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GIT_TOKEN }}
        branch: bundles

    - name: Actions for Discord
      id: discord_notification
      uses: Ilshidur/action-discord@0.3.2

    - name: Discord notification
      if: steps.check_changes.outputs.stdout != '' && steps.check_changes.outputs.stdout.includes('.json') && secrets.DISCORD_WEBHOOK
      env:
        DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      run: |
        echo 'The following .json files have been updated: *.json\n\n[View Commits](https://github.com/${{ github.repository }}/compare/${{ github.event.before }}...${{ github.sha }})' >> $GITHUB_ENV

    - name: Trigger Discord notification
      if: steps.discord_notification.outputs.status == 'success'
      run: curl -X POST -H "Content-Type: application/json" -d "{\"content\": \"$(cat $GITHUB_ENV)\"}" $DISCORD_WEBHOOK

    # Pause the workflow for one minute before running again
    - name: Pause
      if: matrix.minutes != 5
      run: sleep 60
